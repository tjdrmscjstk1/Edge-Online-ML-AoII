# MySQL 설정 예시 (이 파일을 .env로 복사 후 값 채우기)
# gateway.py / 모니터링 앱 실행 전: export $(cat server/mysql_example.env | xargs) 또는 .env 로드

# 먼저 MySQL에서 DB 생성: CREATE DATABASE aoii CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 같은 PC에서 MySQL 쓰면 아래 값 그대로 두고, 비밀번호만 채우면 됨
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
# 여기를 MySQL 로그인 비밀번호로 바꾸기 (mysql -u root -p 할 때 쓰는 비밀번호)
MYSQL_PASSWORD=sungmin0417
MYSQL_DATABASE=aoii
